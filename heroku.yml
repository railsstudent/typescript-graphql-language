setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: ./Dockerfile
  config:
    RAILS_ENV: development
    FOO: bar
release:
  image: web
  command: ['npm run typeorm migration:run']

run:
  web: npm start